<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for?(:title) ? yield(:title) + ' | Candy Crash' : 'Candy Crash - UK Driving Theory Preparation' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Master your UK driving theory test with Candy Crash - comprehensive courses, practice quizzes, and expert instruction.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
        <%= link_to "Candy Crash", root_path, class: "navbar-brand fw-bold" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "Courses", courses_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "About", about_path, class: "nav-link" %>
            </li>
          </ul>
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                  <i class="bi bi-person-circle"></i> <%= current_user.full_name %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                  <li>
                    <%= link_to student_dashboard_path, class: "dropdown-item" do %>
                      <i class="bi bi-speedometer2"></i> Dashboard
                    <% end %>
                  </li>
                  <% if current_user.instructor_or_admin? %>
                    <li>
                      <%= link_to instructor_dashboard_path, class: "dropdown-item" do %>
                        <i class="bi bi-mortarboard"></i> Instructor Panel
                      <% end %>
                    </li>
                  <% end %>
                  <% if current_user.admin? %>
                    <li>
                      <%= link_to admin_dashboard_path, class: "dropdown-item" do %>
                        <i class="bi bi-gear"></i> Admin Panel
                      <% end %>
                    </li>
                  <% end %>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "dropdown-item" do %>
                      <i class="bi bi-box-arrow-right"></i> Sign Out
                    <% end %>
                  </li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Sign In", new_user_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-light btn-sm ms-2" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice || alert %>
      <div class="container mt-3">
        <% if notice %>
          <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= notice %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
          </div>
        <% end %>
        <% if alert %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= alert %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
          </div>
        <% end %>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="flex-grow-1">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white mt-5 py-4">
      <div class="container">
        <div class="row">
          <div class="col-md-4">
            <h5>Candy Crash</h5>
            <p class="small">Your complete UK driving theory preparation platform.</p>
          </div>
          <div class="col-md-4">
            <h5>Quick Links</h5>
            <ul class="list-unstyled">
              <li><%= link_to "Courses", courses_path, class: "text-white-50" %></li>
              <li><%= link_to "About", about_path, class: "text-white-50" %></li>
            </ul>
          </div>
          <div class="col-md-4">
            <h5>PRIMROSE Protocol</h5>
            <p class="small">Based on the PRIMROSE protocol for effective driving education.</p>
          </div>
        </div>
        <hr class="my-3 bg-secondary">
        <p class="text-center small mb-0">&copy; <%= Time.current.year %> Candy Crash. All rights reserved.</p>
      </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
